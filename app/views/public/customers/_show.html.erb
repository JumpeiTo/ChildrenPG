<h1>会員詳細</h1>

<%= image_tag customer.get_profile_image(200,280) %><br>
<h4 class="mt-1 mb-3"><%= customer.nickname %></h4>
<% if customer == current_customer %>
  <p><%= customer.email %></p>
  <p><%= customer.name %></p>
<% end %>
<% publish = customer.is_hidden? ? '非公開' : '公開' %>
公開設定: <span style="color: <%= customer.is_hidden? ? '#6c757d' : '#89CA25' %>;"><%= publish %></span>

<div class="d-flex align-items-center my-3">
  <% if customer == current_customer && customer.email != "guest@example.com"%>
    <%= link_to "編集する", edit_customers_path, class: "btn btn-green mr-3 px-4" %>
    <%= link_to "退会する", customers_check_path, class: "btn btn-sm btn-red mt-1" %>
  <% end %>
</div>